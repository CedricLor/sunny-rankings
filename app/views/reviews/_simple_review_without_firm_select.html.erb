<h3 class="text-center">How is gender equality doing at <%= "#{@firm.name}?" %></h3>
<hr/>
<%= simple_form_for [@firm, @review] do |r| %>
  <%= render "reviews/review_form_master_content", r: r %>
<% end %>

<%#= form_for [@firm, @review], html: {novalidate: "novalidate", class: "simple_form new_review", id: "new_review_ff"} do |r| %>
  <%#= render "reviews/review_form_master_content_ff", r: r %>
<%# end %>